services:
  bot:
    build: .
    restart: always
    env_file: .env
    environment:
      - PYTHONUNBUFFERED=1
    ports:
      - "8080:8080"
    volumes:
      - ./credentials_diman.json:/app/credentials_diman.json:ro
      - ./credentials_partner.json:/app/credentials_partner.json:ro
